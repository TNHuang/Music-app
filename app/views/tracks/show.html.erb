<h1>Track: <%= @track.name %>'s stat</h1>

<table >
  <tr>belongs to the album: <strong><%= @track.album.name %></strong></tr>

  <tr>maybe add into lyric later</tr>

</table>

<br>
<td><%= link_to "edit track", edit_track_url(@track), method: :get %></td>
<br>
<td><%= button_to "delete track", track_url(@track), method: :delete %></td>
<br>
<td><%= link_to "back to track album page", album_url(@track.album) %></td>
<br>
<td><%= link_to "back to track band page", band_url(@track.album.band) %></td>

